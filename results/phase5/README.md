# results/phase5/

Offline routing matrix (`worker_mode=simulated`).

| File | Role |
| --- | --- |
| `routing_matrix.csv` | 4 patterns × 3 strategies |
| `SURPRISE.md` | Where prefix-aware loses + hypothesis |

Reproduce: `uv run python benchmarks/run_routing_matrix.py`
