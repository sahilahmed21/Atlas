# scripts/

One-shot helpers (download model, export CSV, sync Colab artifacts). Prefer small scripts over notebooks for repeatable sweeps.

Always run via uv:

```powershell
uv run python scripts/<name>.py
```
