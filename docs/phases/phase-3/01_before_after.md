# Phase 3 — Before/after chart

**Status: not run.** `results/phase3/naive_vs_vllm.png` does not exist because the GPU vLLM
environment, baseline-compatible load sweep, and Phase 2 simulations are pending.

When Phase 3 runs, embed the chart here and caption it with:

- exact vLLM version/commit and model revision;
- hardware, dtype, GPU memory setting, and load protocol;
- all concurrency/sequence points included or excluded;
- whether TTFT is real streaming TTFT or a documented proxy;
- an explanation of differences limited to what the measurements and source diff support.
